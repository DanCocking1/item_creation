import 'package:flutter/material.dart';
class CharacterCreator extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Character Creator part1'),
      ),
    );
  }
}
